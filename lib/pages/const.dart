import 'package:flutter/material.dart';

var backgroundColor = const Color.fromARGB(255, 0, 43, 91);
const primaryColor = Color.fromARGB(255, 1, 4, 31);
const secondaryColor = Colors.black38;
const darkColor = Colors.blueGrey;
const bodyTextColor = Colors.white;
const bgColor = Color.fromARGB(255, 0, 43, 91);
const defaultPadding = 20.0;
const defaultDuration = Duration(seconds: 1); // we use it on our animation
const maxWidth = 1440.0; // max width of our web
var tabTextStyle = const TextStyle(color: Colors.white);
